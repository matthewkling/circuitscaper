% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cs-all-to-one.R
\name{cs_all_to_one}
\alias{cs_all_to_one}
\title{All-to-One Circuitscape Analysis}
\usage{
cs_all_to_one(
  resistance,
  locations,
  resistance_is = "resistances",
  four_neighbors = FALSE,
  solver = "cg+amg",
  output_dir = NULL,
  verbose = FALSE
)
}
\arguments{
\item{resistance}{A \link[terra:SpatRaster-class]{terra::SpatRaster} or file path. The resistance (or
conductance) surface. Higher values represent greater resistance to
movement. Use the \code{resistance_is} argument if your surface represents
conductances instead.}

\item{locations}{Focal node locations, provided as any of:
\itemize{
\item A \link[terra:SpatRaster-class]{terra::SpatRaster} with positive integer IDs identifying each node.
Cells with value 0 or \code{NA} are not treated as focal nodes.
\item A file path to a raster file (e.g., \code{.tif}, \code{.asc}).
\item A two-column matrix or data.frame of x/y coordinates. Each row becomes
a focal node, auto-assigned IDs 1, 2, 3, ... in row order. Coordinates
are snapped to the nearest cell of the \code{resistance} raster. See
\code{\link[=cs_locations]{cs_locations()}}.
}}

\item{resistance_is}{Character. Whether the resistance surface represents
\code{"resistances"} (default) or \code{"conductances"}.}

\item{four_neighbors}{Logical. Use 4-neighbor (rook) connectivity instead of
8-neighbor (queen). Default \code{FALSE}.}

\item{solver}{Character. Solver to use: \code{"cg+amg"} (default) or \code{"cholmod"}.}

\item{output_dir}{Optional character path. If provided, output files persist
there. Default \code{NULL} uses a temporary directory that is cleaned up
automatically.}

\item{verbose}{Logical. Print Circuitscape solver output. Default \code{FALSE}.}
}
\value{
A \link[terra:SpatRaster-class]{terra::SpatRaster} with the following layers:
\describe{
\item{cumulative_current}{Current flow summed across all iterations.}
\item{curmap_\emph{N}}{Per-node current map for focal node \emph{N}, where
\emph{N} is the integer node ID from the \code{locations} raster. One layer
per focal node.}
}
}
\description{
For each focal node in turn, inject current at all other focal nodes and
ground that single node.
}
\details{
All-to-one mode iterates over each focal node. In each iteration, all other
focal nodes are injected with 1 amp of current each, and the focal node is
connected to ground. This produces a current map showing how current
converges on that node from across the landscape.

This mode is useful for identifying the most accessible or reachable sites
in the network, emphasizing current flow toward each ground node. The
cumulative map sums across all iterations and highlights cells that are
important for connectivity across the full set of nodes.
}
\examples{
\dontrun{
library(terra)
res <- rast(nrows = 10, ncols = 10, vals = runif(100, 1, 10))
coords <- matrix(c(-140, 70, -60, 70, -100, 30), ncol = 2, byrow = TRUE)
result <- cs_all_to_one(res, coords)
plot(result)
}

}
\references{
Circuitscape user guide:
\url{https://docs.circuitscape.org/Circuitscape.jl/latest/usage/}
}
\seealso{
\code{\link[=cs_pairwise]{cs_pairwise()}}, \code{\link[=cs_one_to_all]{cs_one_to_all()}}, \code{\link[=cs_advanced]{cs_advanced()}}, \code{\link[=cs_setup]{cs_setup()}}
}
