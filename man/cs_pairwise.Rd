% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cs-pairwise.R
\name{cs_pairwise}
\alias{cs_pairwise}
\title{Pairwise Circuitscape Analysis}
\usage{
cs_pairwise(
  resistance,
  locations,
  resistance_is = "resistances",
  four_neighbors = FALSE,
  avg_resistances = FALSE,
  short_circuit = NULL,
  included_pairs = NULL,
  write_voltage = FALSE,
  cumulative_only = TRUE,
  source_strengths = NULL,
  solver = "cg+amg",
  output_dir = NULL,
  verbose = FALSE
)
}
\arguments{
\item{resistance}{A \link[terra:SpatRaster-class]{terra::SpatRaster} or file path. The resistance (or
conductance) surface. Higher values represent greater resistance to
movement. Use the \code{resistance_is} argument if your surface represents
conductances instead.}

\item{locations}{Focal node locations, provided as any of:
\itemize{
\item A \link[terra:SpatRaster-class]{terra::SpatRaster} (or \code{raster::RasterLayer}) with positive integer IDs
identifying each node. Cells with value 0 or \code{NA} are not treated as focal
nodes.
\item A file path to a raster file (e.g., \code{.tif}, \code{.asc}).
\item A two-column matrix or data.frame of x/y coordinates. Each row becomes
a focal node, auto-assigned IDs 1, 2, 3, ... in row order. Coordinates
are snapped to the nearest cell of the \code{resistance} raster.
}}

\item{resistance_is}{Character. Whether the resistance surface represents
\code{"resistances"} (default) or \code{"conductances"}.}

\item{four_neighbors}{Logical. Use 4-neighbor (rook) connectivity instead of
8-neighbor (queen). Default \code{FALSE}.}

\item{avg_resistances}{Logical. When using 8-neighbor connectivity, compute
the resistance of diagonal connections as the average of the two cells
rather than their sum. Default \code{FALSE} (Circuitscape default). Ignored when
\code{four_neighbors = TRUE}.}

\item{short_circuit}{Optional \link[terra:SpatRaster-class]{terra::SpatRaster} or file path. Raster
identifying short-circuit regions (aka polygons). Cells sharing the same
positive integer value are treated as short-circuit regions with zero
resistance between them. Default \code{NULL} (no short-circuit regions).}

\item{included_pairs}{Optional character file path. A text file specifying
which pairs of focal nodes to include or exclude from analysis. See the
Circuitscape documentation for the file format. Default \code{NULL} (all pairs).}

\item{write_voltage}{Logical. Write voltage maps. Default \code{FALSE}. When
\code{TRUE}, per-iteration voltage layers (named \code{voltage_1}, \code{voltage_2}, ...)
are included in the output raster.}

\item{cumulative_only}{Logical. If \code{TRUE} (default), only the cumulative
current map is returned. If \code{FALSE}, per-iteration current layers (named
\code{current_1}, \code{current_2}, ...) are also included. Use with caution for
large numbers of focal nodes, as this can produce many layers.}

\item{source_strengths}{Optional. Variable current injection strengths for
each focal node. Can be:
\itemize{
\item A numeric vector with one value per focal node (in the same order as the
locations input). Node IDs are assigned 1, 2, 3, ... matching the order.
\item A character file path to a tab-delimited text file with two columns:
node ID and strength in amps. Nodes not listed default to 1 amp.
Default \code{NULL} (all nodes inject 1 amp).
}}

\item{solver}{Character. Solver to use: \code{"cg+amg"} (default) or \code{"cholmod"}.}

\item{output_dir}{Optional character path. If provided, output files persist
there. Default \code{NULL} uses a temporary directory that is cleaned up
automatically.}

\item{verbose}{Logical. Print Circuitscape solver output. Default \code{FALSE}.}
}
\value{
A named list with:
\describe{
\item{current_map}{A \link[terra:SpatRaster-class]{terra::SpatRaster}. By default contains a single
\code{cumulative_current} layer (current flow summed across all pairs). When
\code{cumulative_only = FALSE}, additional per-pair layers are included
(e.g., \code{current_1_2}, \code{current_1_3}). When \code{write_voltage = TRUE},
per-pair voltage layers are included (e.g., \code{voltage_1_2},
\code{voltage_1_3}).}
\item{resistance_matrix}{A symmetric numeric matrix of pairwise effective
resistances between focal nodes, with node IDs as row and column names.}
}
}
\description{
Compute pairwise effective resistances and cumulative current flow between
all pairs of focal nodes.
}
\details{
Pairwise mode iterates over every unique pair of focal nodes. For each pair,
one node is injected with 1 amp of current and the other is connected to
ground. The effective resistance between the pair is recorded, and the
resulting current flow is accumulated across all pairs into a cumulative
current map that highlights important movement corridors.

This is the most common Circuitscape mode and is typically used to quantify
connectivity between discrete habitat patches or populations. The resistance
matrix can be used as a distance metric in analyses such as isolation by
resistance.
}
\examples{
\dontshow{if (circuitscaper:::julia_check()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
library(terra)
res <- rast(system.file("extdata/resistance.tif", package = "circuitscaper"))
coords <- matrix(c(10, 40, 40, 40, 10, 10, 40, 10), ncol = 2, byrow = TRUE)
result <- cs_pairwise(res, coords, cumulative_only = FALSE)
plot(result$current_map)
result$resistance_matrix
\dontshow{\}) # examplesIf}
}
\references{
McRae, B.H. (2006). Isolation by resistance. \emph{Evolution}, 60(8),
1551--1561. \doi{10.1111/j.0014-3820.2006.tb00500.x}

Circuitscape.jl: \url{https://docs.circuitscape.org/Circuitscape.jl/latest/}
}
\seealso{
\code{\link[=cs_one_to_all]{cs_one_to_all()}}, \code{\link[=cs_all_to_one]{cs_all_to_one()}}, \code{\link[=cs_advanced]{cs_advanced()}}, \code{\link[=cs_setup]{cs_setup()}}
}
