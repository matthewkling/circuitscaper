% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/os-run.R
\name{os_run}
\alias{os_run}
\title{Run Omniscape Moving-Window Connectivity Analysis}
\usage{
os_run(
  resistance,
  radius,
  source_strength = NULL,
  block_size = 1L,
  source_threshold = 0,
  resistance_is = "resistances",
  calc_normalized_current = TRUE,
  calc_flow_potential = TRUE,
  condition = NULL,
  condition_type = NULL,
  parallelize = FALSE,
  julia_threads = 2L,
  solver = "cg+amg",
  output_dir = NULL,
  verbose = FALSE
)
}
\arguments{
\item{resistance}{A \link[terra:SpatRaster-class]{terra::SpatRaster} or file path. The resistance (or
conductance) surface.}

\item{radius}{Numeric. Moving window radius in pixels.}

\item{source_strength}{Optional \link[terra:SpatRaster-class]{terra::SpatRaster} or file path. Source
strength weights. If \code{NULL} (default), all non-nodata pixels are treated as
sources with equal weight.}

\item{block_size}{Integer. Aggregation block size for source points. Default
\code{1} (no aggregation). Increasing this significantly speeds computation.}

\item{source_threshold}{Numeric. Minimum source strength to include a pixel.
Default \code{0}.}

\item{resistance_is}{Character. Whether the resistance surface represents
\code{"resistances"} (default) or \code{"conductances"}.}

\item{calc_normalized_current}{Logical. Compute normalized current flow.
Default \code{TRUE}.}

\item{calc_flow_potential}{Logical. Compute flow potential. Default \code{TRUE}.}

\item{condition}{Optional \link[terra:SpatRaster-class]{terra::SpatRaster} or file path. Conditional layer
for targeted connectivity analysis.}

\item{condition_type}{Character. Determines how the condition layer is used.
Only relevant if \code{condition} is provided. See the Omniscape documentation
for options.}

\item{parallelize}{Logical. Use Julia multithreading. Default \code{FALSE}.
Julia's thread count is fixed at startup. If Julia was already initialized
without enough threads, a warning is issued. To avoid this, call
\code{\link[=cs_setup]{cs_setup()}} with the \code{threads} argument at the start of your session.}

\item{julia_threads}{Integer. Number of Julia threads if \code{parallelize = TRUE}. Default \code{2}. Ignored if Julia is already running with fewer threads.}

\item{solver}{Character. Solver to use: \code{"cg+amg"} (default) or \code{"cholmod"}.}

\item{output_dir}{Optional character path. If provided, output files persist
there. Default \code{NULL} uses a temporary directory.}

\item{verbose}{Logical. Print Omniscape output. Default \code{FALSE}.}
}
\value{
A \link[terra:SpatRaster-class]{terra::SpatRaster} with named layers. Possible layers depending on
options:
\describe{
\item{cumulative_current}{Cumulative current flow.}
\item{flow_potential}{Flow potential (if \code{calc_flow_potential = TRUE}).}
\item{normalized_current}{Normalized current flow (if
\code{calc_normalized_current = TRUE}).}
}
}
\description{
Performs an Omniscape analysis, computing omnidirectional landscape
connectivity using a moving window approach based on circuit theory.
}
\examples{
\dontrun{
library(terra)
res <- rast(nrows = 50, ncols = 50, vals = runif(2500, 1, 10))
result <- os_run(res, radius = 10)
plot(result)
plot(result[["normalized_current"]])
}

}
\references{
Omniscape documentation:
\url{https://docs.circuitscape.org/Omniscape.jl/latest/}
}
\seealso{
\code{\link[=cs_pairwise]{cs_pairwise()}}, \code{\link[=cs_setup]{cs_setup()}}
}
