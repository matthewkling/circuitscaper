% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cs-advanced.R
\name{cs_advanced}
\alias{cs_advanced}
\title{Advanced Circuitscape Analysis}
\usage{
cs_advanced(
  resistance,
  source,
  ground,
  resistance_is = "resistances",
  ground_is = "resistances",
  use_unit_currents = FALSE,
  use_direct_grounds = FALSE,
  short_circuit = NULL,
  source_ground_conflict = "keepall",
  four_neighbors = FALSE,
  avg_resistances = FALSE,
  solver = "cg+amg",
  output_dir = NULL,
  verbose = FALSE
)
}
\arguments{
\item{resistance}{A \link[terra:SpatRaster-class]{terra::SpatRaster} or file path. The resistance (or
conductance) surface. Higher values represent greater resistance to
movement.}

\item{source}{A \link[terra:SpatRaster-class]{terra::SpatRaster} or file path. Source current strengths
(amps per cell). Cells with value 0 or NA are not sources.}

\item{ground}{A \link[terra:SpatRaster-class]{terra::SpatRaster} or file path. Ground node values.
Interpretation depends on \code{ground_is}: resistances to ground (default)
or conductances to ground. Cells with value 0 or NA are not grounds.}

\item{resistance_is}{Character. Whether the resistance surface represents
\code{"resistances"} (default) or \code{"conductances"}.}

\item{ground_is}{Character. Whether the ground raster values represent
\code{"resistances"} (default) or \code{"conductances"} to ground.}

\item{use_unit_currents}{Logical. If \code{TRUE}, all current sources are set to
1 amp regardless of the values in the source raster. Default \code{FALSE}.}

\item{use_direct_grounds}{Logical. If \code{TRUE}, all ground nodes are tied
directly to ground (zero resistance), regardless of the values in the
ground raster. Default \code{FALSE}.}

\item{short_circuit}{Optional \link[terra:SpatRaster-class]{terra::SpatRaster} or file path. Raster
identifying short-circuit regions (aka polygons). Cells sharing the same
positive integer value are treated as short-circuit regions with zero
resistance between them. Default \code{NULL} (no short-circuit regions).}

\item{source_ground_conflict}{Character. How to resolve cells that appear in
both the source and ground rasters: \code{"keepall"} (default, keep both),
\code{"rmvsrc"} (remove source), \code{"rmvgnd"} (remove ground), or \code{"rmvall"}
(remove both).}

\item{four_neighbors}{Logical. Use 4-neighbor (rook) connectivity instead of
8-neighbor (queen). Default \code{FALSE}.}

\item{avg_resistances}{Logical. When using 8-neighbor connectivity, compute
the resistance of diagonal connections as the average of the two cells
rather than their sum. Default \code{FALSE} (Circuitscape default). Ignored when
\code{four_neighbors = TRUE}.}

\item{solver}{Character. Solver to use: \code{"cg+amg"} (default) or \code{"cholmod"}.}

\item{output_dir}{Optional character path. If provided, output files persist
there. Default \code{NULL} uses a temporary directory.}

\item{verbose}{Logical. Print Circuitscape solver output. Default \code{FALSE}.}
}
\value{
A \link[terra:SpatRaster-class]{terra::SpatRaster} with the following layers:
\describe{
\item{current}{Current density at each cell.}
\item{voltage}{Voltage at each cell. Voltage is analogous to movement
probability and decreases with distance from sources.}
}
}
\description{
Solve a single circuit with user-specified source and ground layers.
}
\details{
Unlike the other Circuitscape modes, advanced mode does not iterate over
focal nodes. Instead, the user provides explicit source current and ground
conductance rasters, and a single circuit is solved. This gives full control
over the current injection pattern and is useful for modeling specific
scenarios such as directional movement between a defined source area and
destination.
}
\examples{
\donttest{
library(terra)
res <- rast(system.file("extdata/resistance.tif", package = "circuitscaper"))
origin <- rast(system.file("extdata/source.tif", package = "circuitscaper"))
dest <- rast(system.file("extdata/ground.tif", package = "circuitscaper"))
result <- cs_advanced(res, origin, dest, ground_is = "conductances")
plot(result)
}

}
\references{
Circuitscape user guide:
\url{https://docs.circuitscape.org/Circuitscape.jl/latest/usage/}
}
\seealso{
\code{\link[=cs_pairwise]{cs_pairwise()}}, \code{\link[=cs_one_to_all]{cs_one_to_all()}}, \code{\link[=cs_all_to_one]{cs_all_to_one()}}, \code{\link[=cs_setup]{cs_setup()}}
}
