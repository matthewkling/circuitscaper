% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/os-compare.R
\name{os_compare}
\alias{os_compare}
\title{Compare Two Omniscape Runs}
\usage{
os_compare(baseline, future, metric = "normalized_current")
}
\arguments{
\item{baseline}{A \link[terra:SpatRaster-class]{terra::SpatRaster} result from \code{\link[=os_run]{os_run()}}.}

\item{future}{A \link[terra:SpatRaster-class]{terra::SpatRaster} result from \code{\link[=os_run]{os_run()}}.}

\item{metric}{Character. Layer name to compare. Default
\code{"normalized_current"}.}
}
\value{
A \link[terra:SpatRaster-class]{terra::SpatRaster} with two layers:
\describe{
\item{difference}{\code{future - baseline}}
\item{ratio}{\code{future / baseline}}
}
}
\description{
Convenience function for comparing two Omniscape results, e.g., current vs.
future climate scenarios. Computes the difference and ratio between a
specified layer from each run.
}
\examples{
\dontrun{
baseline <- os_run(resistance_current, radius = 50)
future <- os_run(resistance_future, radius = 50)
comparison <- os_compare(baseline, future)
plot(comparison[["difference"]])
plot(comparison[["ratio"]])
}

}
\seealso{
\code{\link[=os_run]{os_run()}}
}
