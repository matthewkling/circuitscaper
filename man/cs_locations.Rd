% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/locations.R
\name{cs_locations}
\alias{cs_locations}
\title{Create a Focal Node Raster from Coordinates}
\usage{
cs_locations(coords, resistance)
}
\arguments{
\item{coords}{A two-column matrix or data.frame of x and y coordinates.
Each row represents one focal node. IDs are assigned sequentially
(1, 2, 3, ...) based on row order.}

\item{resistance}{A \link[terra:SpatRaster-class]{terra::SpatRaster} used as a template for extent,
resolution, and CRS. The output raster will match this exactly.}
}
\value{
A \link[terra:SpatRaster-class]{terra::SpatRaster} with 0 background and positive integer IDs at
the cells nearest each coordinate. This can be passed directly to the
\code{locations} argument of any \verb{cs_*} function.
}
\description{
Convert a set of point coordinates into a focal node raster suitable for
use with \code{\link[=cs_pairwise]{cs_pairwise()}}, \code{\link[=cs_one_to_all]{cs_one_to_all()}}, and \code{\link[=cs_all_to_one]{cs_all_to_one()}}. Each
point is snapped to the nearest cell of the resistance raster and assigned
a sequential integer ID.
}
\details{
Coordinates are snapped to the nearest raster cell center using
\code{\link[terra:xyCellFrom]{terra::cellFromXY()}}. If two points snap to the same cell, an error is
raised. Points that fall outside the raster extent also produce an error.

This function is called internally when you pass coordinates directly to
\code{cs_pairwise()}, \code{cs_one_to_all()}, or \code{cs_all_to_one()}. Use it
explicitly if you want to inspect or modify the focal node raster before
running an analysis.
}
\examples{
\dontrun{
library(terra)
res <- rast(nrows = 10, ncols = 10, vals = runif(100, 1, 10))

coords <- matrix(c(-140, 70,
                    -100, 30,
                     -60, 50), ncol = 2, byrow = TRUE)
locs <- cs_locations(coords, res)
plot(locs)

# Equivalent to passing coords directly:
result <- cs_pairwise(res, coords)
}

}
\seealso{
\code{\link[=cs_pairwise]{cs_pairwise()}}, \code{\link[=cs_one_to_all]{cs_one_to_all()}}, \code{\link[=cs_all_to_one]{cs_all_to_one()}}
}
